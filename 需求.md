
- base 

    1. 更改全局/etc/environment设置默认编辑器（默认使用vim，如果用户没有安装则安装，如果用户已经安装了nvim或者nano则设置为nano）
    2. 检测是否开启32位源,没有的话开启
    3. 安装基础字体wqy-zenhei noto-fonts noto-fonts-emoji
    4. 添加archlinuxcn源，并同步数据库和安装密钥
    5. 添加archlinuxcn之后可以直接pacman安装aur助手，yay和paru都装上吧
    6. 

- basic

    1. 检测是否为btrfs文件系统，如果是的话安装快照程序snapper、btrfs-assistant。检测是否使用grub，如果使用的话配置自动生成快照启动项和覆盖文件系统（这里需要重启，但是先不重启，继续接下去的操作）
    2. 安装音视频固件和服务
    3. 安装中文输入法
    4. 蓝牙（使用blueman就好了）
    5. 性能模式切换



    3. 使用我的git仓库里的.config文件配置niri

